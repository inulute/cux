---
description: Add or refresh the currently logged-in Claude Code account in cux
argument-hint: "[--slot N]"
allowed-tools: Bash(cux add:*)
---

# /cux:add

Registers the currently logged-in Claude Code account with cux, or
refreshes its saved credentials if it is already managed.

```bash
cux add $ARGUMENTS
```
