---
description: Show or update cux configuration
argument-hint: "show | keys | set <key> <value>"
allowed-tools: Bash(cux config:*)
---

# /cux:config

Shows or updates cux configuration.

```bash
cux config $ARGUMENTS
```
